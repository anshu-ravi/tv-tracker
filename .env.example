# Copy to .env.local and fill in. Never commit real values (.env* is gitignored).

# TMDB — the API Read Access Token (v4 "bearer" token, starts with eyJ...). Server-only.
TMDB_API_KEY=

# Supabase — public config, safe to expose to the browser.
NEXT_PUBLIC_SUPABASE_URL=https://ermhfiofisjsrniccqlv.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_...
